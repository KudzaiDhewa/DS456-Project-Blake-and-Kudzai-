total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits))
library_summary
#| label: fig-norm-dist-repo
#| fig-cap: "Normalized Category Distribution by Repository. The pattern holds across diverse domains."
#| echo: false
knitr::include_graphics("visualizations/normalized_category_distribution_by_lib.png")
#| code-fold: true
commits <- read.csv("Data/FINAL_CSS_WITH_PREDICTION_TIMESERIES.csv")
library(dplyr)
library_summary <- commits %>%
group_by(repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits)) %>%
head(9)
library_summary
library(dplyr)
library(dplyr)
library(dplyr)
library(gt)
install.packages("gt")
library(dplyr)
library(gt)
library_summary_lang <- commits %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
1
),
.groups = "drop"
)
library_summary_lang %>%
gt() %>%
cols_label(
language = "Language",
repo = "Library",
total_commits = "Total Commits",
year_range = "Year Range",
avg_commits_per_year = "Avg Commits / Year"
) %>%
fmt_number(
columns = c(total_commits, avg_commits_per_year),
decimals = 1
) %>%
tab_header(
title = md("**Commit Activity by Library and Language**"),
subtitle = "Maintenance activity normalized by project age"
) %>%
tab_style(
style = cell_text(weight = "bold"),
locations = cells_column_labels(everything())
) %>%
opt_table_outline() %>%
opt_align_table_header(align = "left")
library_summary_lang <- commits %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
1
),
.groups = "drop"
)
library_summary_lang %>%
gt() %>%
cols_label(
language = "Language",
repo = "Library",
total_commits = "Total Commits",
year_range = "Year Range",
avg_commits_per_year = "Avg Commits / Year"
) %>%
fmt_number(
columns = c(total_commits, avg_commits_per_year),
decimals = 1
) %>%
tab_header(
title = md("**Commit Activity by Library and Language**"),
subtitle = "Maintenance activity normalized by project age"
) %>%
tab_style(
style = cell_text(weight = "bold"),
locations = cells_column_labels(everything())
) %>%
opt_table_outline() %>%
opt_align_table_header(align = "left")
install.packages("xfun")
library_summary_lang %>%
gt() %>%
cols_label(
language = "Language",
repo = "Library",
total_commits = "Total Commits",
year_range = "Year Range",
avg_commits_per_year = "Avg Commits / Year"
) %>%
fmt_number(
columns = c(total_commits, avg_commits_per_year),
decimals = 1
) %>%
tab_header(
title = md("**Commit Activity by Library and Language**"),
subtitle = "Maintenance activity normalized by project age"
) %>%
tab_style(
style = cell_text(weight = "bold"),
locations = cells_column_labels(everything())
) %>%
opt_table_outline() %>%
opt_align_table_header(align = "left")
library_summary_lang <- commits %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
1
),
.groups = "drop"
)
library(dplyr)
library(dplyr)
library(gt)
library_summary_lang <- commits %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
1
),
.groups = "drop"
)
library_summary_lang %>%
gt() %>%
cols_label(
language = "Language",
repo = "Library",
total_commits = "Total Commits",
year_range = "Year Range",
avg_commits_per_year = "Avg Commits / Year"
) %>%
fmt_number(
columns = c(total_commits, avg_commits_per_year),
decimals = 1
) %>%
tab_header(
title = md("**Commit Activity by Library and Language**"),
subtitle = "Maintenance activity normalized by project age"
) %>%
tab_style(
style = cell_text(weight = "bold"),
locations = cells_column_labels(everything())
) %>%
opt_table_outline() %>%
opt_align_table_header(align = "left")
#| code-fold: true
commits <- read.csv("Data/FINAL_CSS_WITH_PREDICTION_TIMESERIES.csv")
library(dplyr)
library(dplyr)
library(gt)
library_summary_lang <- commits %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
1
),
.groups = "drop"
) %>%
head(9)
library_summary_lang %>%
gt() %>%
cols_label(
language = "Language",
repo = "Library",
total_commits = "Total Commits",
year_range = "Year Range",
avg_commits_per_year = "Avg. Commits / Year"
) %>%
fmt_number(
columns = c(total_commits, avg_commits_per_year),
decimals = 1
) %>%
tab_header(
title = md("**Commit Activity by Library and Language**"),
subtitle = "Commit volume normalized by project lifespan"
) %>%
tab_style(
style = cell_text(weight = "bold"),
locations = cells_column_labels(everything())
) %>%
tab_style(
style = cell_fill(color = "#eef5ff"),
locations = cells_body(language == "rust")
) %>%
tab_style(
style = cell_fill(color = "#fff4e6"),
locations = cells_body(language == "c")
) %>%
opt_table_outline() %>%
opt_align_table_header(align = "left")
View(commits)
summary_table <- commits_df %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
start_year = min(year, na.rm = TRUE),
end_year = max(year, na.rm = TRUE),
year_range = paste0(start_year, "–", end_year),
avg_commits_per_year = round(
total_commits / (end_year - start_year + 1),
2
),
.groups = "drop"
) %>%
select(
language,
repo,
total_commits,
year_range,
avg_commits_per_year
) %>%
arrange(language, repo)
summary_table <- commits %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
start_year = min(year, na.rm = TRUE),
end_year = max(year, na.rm = TRUE),
year_range = paste0(start_year, "–", end_year),
avg_commits_per_year = round(
total_commits / (end_year - start_year + 1),
2
),
.groups = "drop"
) %>%
select(
language,
repo,
total_commits,
year_range,
avg_commits_per_year
) %>%
arrange(language, repo)
knitr::kable(
summary_table,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Language",
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
summary_table <- commits %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
start_year = min(year, na.rm = TRUE),
end_year = max(year, na.rm = TRUE),
year_range = paste0(start_year, "–", end_year),
avg_commits_per_year = round(
total_commits / (end_year - start_year + 1),
2
),
.groups = "drop"
) %>%
select(
language,
repo,
total_commits,
year_range,
avg_commits_per_year
) %>%
arrange(language, repo) %>%
head(9)
knitr::kable(
summary_table,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Language",
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
summary_table
summary_table
library_summary <- commits %>%
group_by(repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits)) %>%
head(9)
library_summary
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
library_summary
library_summary <- commits %>%
group_by(repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits)) %>%
head(9) %>%
select(repo,total_commits,year_range,avg_commits_per_year)
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
```
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
#| code-fold: true
commits <- read.csv("Data/FINAL_CSS_WITH_PREDICTION_TIMESERIES.csv")
library(dplyr)
library_summary <- commits %>%
group_by(repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits)) %>%
head(9) %>%
select(repo,total_commits,year_range,avg_commits_per_year)
library_summary
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
library(dplyr)
library_summary <- commits %>%
group_by(repo) %>%
summarise(
total_commits = n(),
min_year = min(year, na.rm = TRUE),
max_year = max(year, na.rm = TRUE),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits)) %>%
head(9) %>%
select(repo,total_commits,year_range,avg_commits_per_year)
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
library_summary <- commits %>%
filter(!is.na(year)) %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
min_year = min(year),
max_year = max(year),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits)) %>%
slice_head(n = 9) %>%
select(language, repo, total_commits, year_range, avg_commits_per_year)
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Language",
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
commits <- read.csv("Data/FINAL_CSS_WITH_PREDICTION_TIMESERIES.csv")
library_summary <- commits %>%
filter(!is.na(year)) %>%
group_by(language, repo) %>%
summarise(
total_commits = n(),
min_year = min(year),
max_year = max(year),
year_range = paste0(min_year, "–", max_year),
avg_commits_per_year = round(
total_commits / (max_year - min_year + 1),
2
),
.groups = "drop"
) %>%
arrange(desc(total_commits)) %>%
slice_head(n = 9) %>%
select(language, repo, total_commits, year_range, avg_commits_per_year)
knitr::kable(
library_summary,
caption = "Commit Activity by Language and Repository",
col.names = c(
"Language",
"Repository",
"Total Commits",
"Year Range",
"Avg. Commits / Year"
)
)
